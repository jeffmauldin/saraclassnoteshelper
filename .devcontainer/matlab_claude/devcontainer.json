{
    "name": "DevEnviron Claude Code W/ Matlab",
    "image": "mathworks/matlab:r2024b",
    "features": {
        // Installs a modern Node.js version (latest LTS or specific)
        "ghcr.io/devcontainers/features/node:1": {
            "version": "lts"
        }
    },
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y vim curl && curl -fsSL https://claude.ai/install.sh | bash",
}
